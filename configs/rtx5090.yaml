# RTX 5090 / Ubuntu 24.04 / CUDA 12.8 — overrides on top of default.yaml
llm:
  provider: "anthropic"
  model: "claude-opus-4-7"
  max_chunk_chars: 12000

tts:
  provider: "edge"
  voice: "zh-TW-HsiaoChenNeural"

renderer:
  fps: 30
  ken_burns: true

runtime:
  max_hours: 10.0
  parallelism: 4              # 5090 可同時 render 多 scene
